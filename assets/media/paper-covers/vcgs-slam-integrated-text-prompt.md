# VCGS-SLAM cover prompt

Use case: scientific-educational

Asset type: academic publication cover for a bilingual researcher website, landscape 16:9

Primary request: Create a restrained white-background graphical abstract for the paper VCGS-SLAM, explaining compact 3D Gaussian dense visual SLAM for embedded vision.

Scene/backdrop: clean white scientific-paper canvas with very pale blue-gray grouping panels, generous margins, no photographic background.

Style/medium: precise vector-like academic infographic, crisp sans-serif typography, thin navy outlines, muted cyan/teal accents, small coral only for pruning; visually distinct from a promotional poster.

Composition/framing: one unambiguous left-to-right primary flow across the full landscape. At left, a short RGB-D image sequence enters a voxel grid. In the center, show a compact indoor scene represented by voxel anchors carrying a small number of colored 3D Gaussian ellipsoids. Above and below the central map, show two clearly connected mechanisms: a sliding-window mask removes redundant Gaussians, and residual codebook quantization replaces repeated Gaussian attributes with a small stack of codebook entries. A separate lower tracking lane shows keyframes flowing into local-to-global bundle adjustment with ICP and returning one clean camera trajectory. At right, the compact dense map runs on a small generic embedded compute module and produces a sharp rendered view.

Text (verbatim): "VCGS-SLAM"; "VOXELIZED 3D GAUSSIAN SLAM FOR EMBEDDED VISION"; "RGB-D SEQUENCE"; "VOXEL-ANCHORED GAUSSIANS"; "ONLINE SLIDING-WINDOW MASK"; "RESIDUAL CODEBOOK QUANTIZATION"; "LOCAL-TO-GLOBAL BA + ICP"; "COMPACT DENSE MAP"; "EMBEDDED DEPLOYMENT".

Arrow constraints: use orthogonal or gently curved arrows with one clear direction; every arrow must start at a node boundary and end at the intended next node boundary; no arrow may cross another arrow, pass through text, float without a source, or point backward. Keep the primary mapping flow visually stronger than the lower tracking feedback lane.

Constraints: scientifically plausible depiction of Gaussian ellipsoids and voxel anchors; all listed labels large and readable at web-card size; no equations, no unsupported metrics, no logos, no trademarks, no watermark, no extra text, no decorative circuitry, no sci-fi glow, no excessive gradients.
